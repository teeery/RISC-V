/* ============================================================================
 * breakpoint.c — 软件断点实现
 * ============================================================================
 *
 * 实现 bp_set, bp_remove, bp_remove_at, bp_find, bp_list
 *
 * 断点数组管理：
 *   初始分配一个较小的容量（如 16 个断点），
 *   用满后 realloc 扩容（capacity *= 2）。
 *
 * 注意事项：
 *   1. 写 ebreak 指令到代码段时，代码段的权限可能是 R+X（不可写）。
 *      作为模拟器，你们可以选择忽略权限直接写入 data 缓冲区。
 *      或者在写之前临时放行。
 *
 *   2. 恢复指令后继续执行：需要先单步执行原始指令，
 *      再重新写入 ebreak，然后继续正常执行。
 *
 *   3. 删除断点时：要恢复原始指令。
 */

#include "breakpoint.h"

/* ---- 在这里实现 ---- */
